initSidebarItems({"constant":[["PUBLICKEYBYTES",""],["SECRETKEYBYTES",""],["SIGNATUREBYTES",""]],"fn":[["gen_keypair","`gen_keypair()` randomly generates a secret key and a corresponding public key."],["sign","`sign()` signs a message `m` using the signer's secret key `sk`. `sign()` returns the resulting signed message `sm`."],["verify","`verify()` verifies the signature in `sm` using the signer's public key `pk`. `verify()` returns the message `Some(m)`. If the signature fails verification, `verify()` returns `None`."]],"struct":[["PublicKey","`PublicKey` for signatures"],["SecretKey","`SecretKey` for signatures"]]});